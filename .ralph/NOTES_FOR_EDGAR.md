# Things Edgar & Mija Need to Do Manually

Ralph will update this file when it encounters tasks requiring human action.

---

## Before Ralph Can Start

- [ ] Ensure Node.js 20+ is installed (`node --version`)
- [ ] Ensure pnpm is installed (`pnpm --version`)
- [ ] Ensure Go 1.22+ is installed (`go version`)
- [ ] Ensure Docker is running (for PostgreSQL + Redis dev containers)
- [ ] Ensure protoc (Protocol Buffers compiler) is installed for gRPC
- [ ] Create GitHub repo: `github.com/<org>/mixa-ai` (if not already)

## API Keys Needed (for later phases)

- [ ] OpenAI API key (for embeddings + summarization — Phase 3)
- [ ] Anthropic API key (alternative LLM provider — Phase 3)
- [ ] LemonSqueezy account (for payments — Phase 7)

## Design Decisions Needed

- [ ] Final icon/logo for Mixa AI desktop app
- [ ] Color palette / brand guidelines for the browser chrome
- [ ] Decision on Q6: PostgreSQL vs SQLite+sqlite-vss for local-only mode

---

<!-- Ralph will append items below as it discovers them -->
